#include <stdio.h>

int main() {

	printf("\nTo test the word count program.\n"
		"The inputs that should be used are special character (punctuation),\n"
		"and escape characters like newlines, returns, backspaces, tabs, etc...\n\n");

	return 0;
}
