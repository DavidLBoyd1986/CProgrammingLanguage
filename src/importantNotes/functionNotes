
Example Functions:
	itoa - functionExamples/iota.c
	reverse - /functionExamples/iota.c
		reverses string in place

main() Function:

	Return 0 if main() was successful.

	Return non-zero (positive int) to indicate failure.

string.h Functions:

	int strlen(char s[])
	int strcmp(str1, str2)

	IMPORTANT: strcmp() returns 0 if true, which is false in c
	Must do - if (strcp(str1, str2) == 0) to get correct functionality

stdio.h Functions:

	fgets(<var_to_save_to>, <size>, stdin)
	fgets(<var_to_save_to>, <size>, <FILE>)

	fopen(<path_to_file>, "r")
	
	fclose(file);

	printf("testinput = %s", testinput);

	getchar()
	char character = getchar();

	putchar()
	putchar(character);

	scanf("%i", &test_input); // & is a pointer to memory address of test_input variable



